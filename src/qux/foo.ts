export const foo = (aabb: number, xaa: number) => xaa * aabb;

export const oxk = 123;

export const zazaza = (zz: number, zzz: number) => (zz + zzz) / (zz * 12);

console.log(`${oxk} -> ${zazaza}`);
