import { plus /* minus */ } from './add';

test('should add one plus two', () => {
  // expect(plus(3, 2)).toBe(5);
  expect(plus(1, 2)).toBe(3);
});

// test('should minus be okey', () => {
//   expect(minus(30, 3)).toBe(27);
// });
